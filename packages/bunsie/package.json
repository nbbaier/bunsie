{
  "name": "bunsie",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "bunsie": "dist/cli.mjs"
  },
  "scripts": {
    "build": "bun run build:cli",
    "build:cli": "tsdown src/cli.ts --format esm --platform node --external bun --out-dir dist --clean && chmod +x dist/cli.mjs"
  },
  "dependencies": {
    "@kitajs/html": "^4"
  },
  "devDependencies": {
    "tsdown": "^0.20.3"
  }
}
